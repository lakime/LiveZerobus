# Caddy reverse proxy with auto Let's Encrypt HTTPS.
# Caddy reads PUBLIC_HOST and ACME_EMAIL from the environment.

{
    email {$ACME_EMAIL}
}

{$PUBLIC_HOST} {
    encode gzip
    reverse_proxy sap-bdc:8000
}
